import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.tt1',
  appName: 'Documentally',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
